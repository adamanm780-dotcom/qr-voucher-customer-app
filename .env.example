# FlowState Wallet — Environment-Variablen (Vorlage)
# Echte Werte gehören in .env (NICHT committen) bzw. in die Vercel Environment Variables.

# --- Supabase (öffentlich, für Browser/Client) ---
SUPABASE_URL=https://uyqjaasrnqkvuhgtnjbj.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_vmm6yHGzsKCteylEfJr6qw_WvkKM_RD

# --- Supabase (GEHEIM, nur Server — niemals in Browser/Git) ---
SUPABASE_SECRET_KEY=sb_secret_HIER_EINFUEGEN

# --- Apple Wallet Signing ---
APPLE_PASS_TYPE_ID=pass.com.lila.gutschein
APPLE_TEAM_ID=4X4Z2XA87V
APPLE_P12_PASSWORD=lila
# Die Zertifikate (pass.p12, wwdr.pem) liegen lokal in certs/ und werden für Vercel
# später als Base64-Env-Variablen hinterlegt (machen wir beim Deploy des Pass-Servers).
